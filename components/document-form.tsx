"use client";import {FormEvent,useState} from "react";import {useRouter} from "next/navigation";import {createClient} from "@/lib/supabase/client";export function DocumentForm({projects}:{projects:{id:number;title:string}[]}){const r=useRouter();const [v,setV]=useState({project_id:"",title:"",type:"other",number:"",url:""});const [error,setError]=useState("");async function submit(e:FormEvent){e.preventDefault();const s=createClient();if(!s)return setError("Supabase не настроен.");const {data,error}=await s.rpc("create_document",{p_project_id:v.project_id?Number(v.project_id):null,p_document_type:v.type,p_document_number:v.number||null,p_title:v.title,p_drive_file_url:v.url||null});if(error)return setError(error.message);const d:any=Array.isArray(data)?data[0]:data;r.push(`/documents`);r.refresh()}return <form onSubmit={submit} className="mx-auto max-w-2xl space-y-5 rounded-2xl border bg-white p-6"><h1 className="text-3xl font-bold">Создание документа</h1><label><span>Наименование *</span><input className="input" value={v.title} onChange={e=>setV({...v,title:e.target.value})} required/></label><label><span>Проект</span><select className="input" value={v.project_id} onChange={e=>setV({...v,project_id:e.target.value})}><option value="">Не выбран</option>{projects.map(p=><option key={p.id} value={p.id}>{p.title}</option>)}</select></label><label><span>Тип</span><input className="input" value={v.type} onChange={e=>setV({...v,type:e.target.value})}/></label><label><span>Номер</span><input className="input" value={v.number} onChange={e=>setV({...v,number:e.target.value})}/></label><label><span>Drive URL</span><input className="input" type="url" value={v.url} onChange={e=>setV({...v,url:e.target.value})}/></label>{error&&<p className="text-red-700">{error}</p>}<button className="rounded-xl bg-red-700 px-5 py-3 text-white">Создать</button></form>}
