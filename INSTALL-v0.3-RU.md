# Установка v0.3

1. Остановить сервер: `Ctrl + C`.
2. Скопировать содержимое архива поверх текущего проекта.
3. Сохранить существующий `.env.local`.
4. В каталоге проекта выполнить:

```powershell
npm.cmd install
npm.cmd run dev
```

Открыть: `http://localhost:3000/projects`
