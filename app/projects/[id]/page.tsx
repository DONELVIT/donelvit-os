import {notFound} from "next/navigation";import {ExternalLink} from "lucide-react";import {StatusBadge} from "@/components/status-badge";import {getProject} from "@/lib/data/projects";export default async function ProjectPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const p=await getProject(Number(id));if(!p)notFound();const fields=[["Клиент",p.client_name],["Объект",p.object_name??"—"],["Этап",p.stage],["Ответственный",p.responsible_person??"Не назначен"],["Срок",p.due_date??"Не установлен"],["Стоимость",p.amount===null?"—":`${p.amount.toLocaleString("ru-RU")} ${p.currency}`]];return <div className="space-y-6"><section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-panel"><div className="flex flex-col justify-between gap-4 md:flex-row md:items-start"><div><p className="text-sm font-semibold text-red-700">{p.project_number??"Без номера"}</p><h1 className="mt-2 max-w-4xl text-3xl font-bold tracking-tight">{p.title}</h1></div><StatusBadge status={p.status}/></div></section><section className="grid gap-6 xl:grid-cols-[1fr_360px]"><div className="rounded-2xl border border-slate-200 bg-white p-6 shadow-panel"><h2 className="text-lg font-semibold">Основные данные</h2><dl className="mt-5 grid gap-5 md:grid-cols-2">{fields.map(([l,v])=><div key={l} className="border-b border-slate-100 pb-4"><dt className="text-xs uppercase tracking-wide text-slate-500">{l}</dt><dd className="mt-2 font-medium">{v}</dd></div>)}</dl></div><aside className="space-y-4"><div className="rounded-2xl border border-slate-200 bg-white p-6 shadow-panel"><h2 className="font-semibold">Интеграции</h2><div className="mt-4 space-y-3">{["Google Drive","Todoist","Документы"].map(n=><button key={n} className="flex w-full items-center justify-between rounded-xl border border-slate-200 px-4 py-3 text-left text-sm font-medium">{n}<ExternalLink size={16}/></button>)}</div></div><div className="rounded-2xl border border-amber-200 bg-amber-50 p-5 text-sm text-amber-900">Редактирование и создание проекта войдут в следующую итерацию.</div></aside></section></div>}