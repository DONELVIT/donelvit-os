import Link from "next/link";import {Plus} from "lucide-react";import {StatusBadge} from "@/components/status-badge";import {getProjects} from "@/lib/data/projects";export default async function ProjectsPage(){const projects=await getProjects();return <div className="space-y-6"><section className="flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><p className="text-sm font-medium text-red-700">Реестр</p><h1 className="mt-1 text-3xl font-bold">Проекты</h1><p className="mt-2 text-slate-500">Проектирование, проверка и техническая экспертиза.</p></div><button className="inline-flex items-center justify-center gap-2 rounded-xl bg-red-700 px-4 py-3 text-sm font-semibold text-white"><Plus size={18}/>Новый проект</button></section><section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-panel"><div className="overflow-x-auto"><table className="min-w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-5 py-4">№ проекта</th><th className="px-5 py-4">Наименование</th><th className="px-5 py-4">Клиент / объект</th><th className="px-5 py-4">Этап</th><th className="px-5 py-4">Срок</th><th className="px-5 py-4">Статус</th></tr></thead><tbody className="divide-y divide-slate-100">{projects.map(p=><tr key={p.id} className="hover:bg-slate-50"><td className="px-5 py-4 font-semibold"><Link href={`/projects/${p.id}`} className="text-red-700">{p.project_number??"Без номера"}</Link></td><td className="max-w-[380px] px-5 py-4 font-medium">{p.title}</td><td className="px-5 py-4"><div>{p.client_name}</div><div className="mt-1 text-xs text-slate-500">{p.object_name??"Объект не указан"}</div></td><td className="px-5 py-4 text-slate-600">{p.stage}</td><td className="px-5 py-4 text-slate-600">{p.due_date??"—"}</td><td className="px-5 py-4"><StatusBadge status={p.status}/></td></tr>)}</tbody></table></div></section></div>}