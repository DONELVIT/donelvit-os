import {notFound} from "next/navigation";import Link from "next/link";import {getObjects} from "@/lib/data/objects";export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const item=(await getObjects()).find(x=>x.id===Number(id));if(!item)notFound();return <div className="space-y-5"><Link href="/objects" className="text-red-700">К реестру</Link><h1 className="text-3xl font-bold">{item.name}</h1><dl className="grid gap-4 rounded-2xl border bg-white p-6 md:grid-cols-2"><div><dt>Клиент</dt><dd>{item.client_name}</dd></div><div><dt>Адрес</dt><dd>{item.address??"—"}</dd></div><div><dt>Проекты</dt><dd>{item.projects_count}</dd></div></dl></div>}
