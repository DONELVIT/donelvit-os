import Link from "next/link";import {notFound} from "next/navigation";import {getObject} from "@/lib/data/objects";export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const item=await getObject(Number(id));if(!item)notFound();return <div className="space-y-5"><div className="flex justify-between"><Link href="/objects">К реестру</Link><Link href={`/objects/${item.id}/edit`} className="rounded-xl bg-red-700 px-4 py-3 text-white">Редактировать</Link></div><h1 className="text-3xl font-bold">{item.name}</h1><p>{item.is_active?"Активный объект":"Архив"}</p><dl className="grid gap-4 rounded-2xl border bg-white p-6 md:grid-cols-2"><div><dt>Клиент</dt><dd>{item.client_name}</dd></div><div><dt>Адрес</dt><dd>{item.address??"—"}</dd></div><div><dt>Проекты</dt><dd>{item.projects_count}</dd></div></dl></div>}
