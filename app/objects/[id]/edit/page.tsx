import {notFound} from "next/navigation";import {ObjectForm} from "@/components/object-form";import {getObject} from "@/lib/data/objects";import {getClients} from "@/lib/data/clients";export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const [item,clients]=await Promise.all([getObject(Number(id)),getClients()]);if(!item)notFound();return <div className="mx-auto max-w-3xl space-y-6"><h1 className="text-3xl font-bold">Редактирование объекта</h1><ObjectForm item={item} clients={clients}/></div>}
