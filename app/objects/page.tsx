import Link from "next/link";import {Building2,Search} from "lucide-react";import {getObjects} from "@/lib/data/objects";export const dynamic="force-dynamic";export default async function ObjectsPage({searchParams}:{searchParams:Promise<{q?:string}>}){const [all,p]=await Promise.all([getObjects(),searchParams]);const q=(p.q??"").toLowerCase();const objects=all.filter(x=>!q||[x.name,x.client_name,x.address].some(v=>v?.toLowerCase().includes(q)));return <div className="space-y-6"><header><p className="text-sm font-semibold text-red-700">Недвижимость</p><h1 className="mt-1 text-3xl font-bold">Объекты</h1><p className="mt-2 text-slate-500">Реестр объектов и их связь с клиентами и проектами.</p></header><form className="rounded-2xl border bg-white p-4 shadow-panel"><label className="relative block"><Search className="absolute left-3 top-3.5 text-slate-400" size={18}/><input name="q" defaultValue={p.q} className="input pl-10" placeholder="Объект, клиент, адрес..."/></label></form><section className="overflow-hidden rounded-2xl border bg-white shadow-panel"><table className="min-w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr>{["Объект","Клиент","Адрес","Класс","Площадь / этажи","Проекты"].map(x=><th key={x} className="px-5 py-4">{x}</th>)}</tr></thead><tbody className="divide-y">{objects.map(x=><tr key={x.id}><td className="px-5 py-4 font-semibold text-red-700">{x.name}</td><td className="px-5 py-4">{x.client_name}</td><td className="px-5 py-4">{x.address??"—"}</td><td className="px-5 py-4">{x.functional_class??"—"}</td><td className="px-5 py-4">{x.total_area_m2??"—"} / {x.floors_count??"—"}</td><td className="px-5 py-4">{x.projects_count}</td></tr>)}{!objects.length&&<tr><td colSpan={6} className="px-5 py-10 text-center text-slate-500">Объекты не найдены.</td></tr>}</tbody></table></section></div>}
