import {notFound} from "next/navigation";import {ProjectWriteGate} from "@/components/auth-ui";import {ClientForm} from "@/components/client-form";import {getClient} from "@/lib/data/clients";export const dynamic="force-dynamic";export default async function EditClientPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const clientId=Number(id);if(!Number.isInteger(clientId)||clientId<=0)notFound();const client=await getClient(clientId);if(!client)notFound();return <ProjectWriteGate nextPath={`/clients/${client.id}/edit`}><div className="mx-auto max-w-5xl space-y-6"><h1 className="text-3xl font-bold">Редактирование клиента</h1><ClientForm client={client}/></div></ProjectWriteGate>}
