import {getDocuments} from "@/lib/data/documents";export const dynamic="force-dynamic";export default async function Page(){const docs=await getDocuments();return <div className="space-y-6"><header><p className="text-sm font-semibold text-red-700">Реестр</p><h1 className="text-3xl font-bold">Документы</h1></header><section className="overflow-hidden rounded-2xl border bg-white shadow-panel"><table className="min-w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr>{["Документ","Тип","Проект","Договор","Ревизия","Статус"].map(x=><th key={x} className="px-5 py-4">{x}</th>)}</tr></thead><tbody className="divide-y">{docs.map(x=><tr key={x.id}><td className="px-5 py-4 font-semibold">{x.drive_file_url?<a className="text-red-700" href={x.drive_file_url} target="_blank">{x.title}</a>:x.title}<p className="text-xs text-slate-500">{x.document_number??"Без номера"}</p></td><td>{x.document_type}</td><td>{x.project_name??"—"}</td><td>{x.contract_number??"—"}</td><td>{x.revision??"—"}</td><td>{x.status}</td></tr>)}{!docs.length&&<tr><td colSpan={6} className="px-5 py-10 text-center text-slate-500">Документы не найдены.</td></tr>}</tbody></table></section></div>}
