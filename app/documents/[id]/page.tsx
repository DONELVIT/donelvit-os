import Link from "next/link";import {notFound} from "next/navigation";import {getDocument} from "@/lib/data/documents";export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const d=await getDocument(Number(id));if(!d)notFound();return <div className="space-y-5"><div className="flex justify-between"><Link href="/documents">К реестру</Link><Link href={`/documents/${d.id}/edit`} className="rounded-xl bg-red-700 px-4 py-3 text-white">Редактировать</Link></div><h1 className="text-3xl font-bold">{d.title}</h1><p>{d.status}</p><p>{d.document_type} · {d.document_number??"Без номера"}</p>{d.drive_file_url&&<a href={d.drive_file_url} target="_blank" className="text-red-700">Открыть Drive-файл</a>}</div>}
