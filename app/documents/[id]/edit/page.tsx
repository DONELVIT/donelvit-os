import {notFound} from "next/navigation";import {DocumentForm} from "@/components/document-form";import {getDocument} from "@/lib/data/documents";import {getProjects} from "@/lib/data/projects";export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const [item,projects]=await Promise.all([getDocument(Number(id)),getProjects()]);if(!item)notFound();return <DocumentForm item={item} projects={projects.map(p=>({id:p.id,title:p.title}))}/>}
