# Harness Support Files

These files support `AGENTS.md` without forcing every session to load detailed policies.

- `memory/INDEX.md` is a small, durable index. Add a detailed topic first, then add one concise pointer to the index.
- `SAFE_OPERATIONS.md` defines actions that require user approval.

Do not store credentials, tokens, personal data, or generated build output here.
